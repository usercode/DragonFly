@model AssetBlock
@inject IAssetStorage Storage

@if (Model.AssetId != null)
{
    Asset asset = await Storage.GetAssetAsync(Model.AssetId.Value);

    <div class="asset-block">
        <img src="@Url.ImageWizard().Asset(asset).AsImage().Resize(1200,900).BuildUrl()">
    </div>
}