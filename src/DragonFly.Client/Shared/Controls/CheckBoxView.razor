<BSInputCheckbox @bind-Value="Value" />


@code {
    [Parameter]
    public bool Value { get; set; }

    [Parameter]
    public Action<bool> ValueChanged { get; set; }

    private void OnChange(bool value)
    {
        if (Value != value)
        {
            Value = value;

            ValueChanged?.Invoke(value);
        }
    }
}
