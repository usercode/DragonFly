@inherits FieldComponent<SlugField, SlugFieldOptions>
@inject ISlugService SlugService

<div class="slug-field">   
    <MudTextField 
        Value="@Field.Value" 
        @onchange="x=> Field.Value = SlugService.Transform(x.Value.ToString())" 
        ReadOnly="@IsReadOnly" />
</div>
