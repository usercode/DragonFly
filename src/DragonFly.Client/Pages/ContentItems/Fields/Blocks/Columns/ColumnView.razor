@if (Column.Width == ColumnWidth.Value)
{
    <FluentNumberField @bind-Value="Column.Value" Style=" width: 6rem;" min="1" max="12" Disabled="IsReadOnly" />
}
<EnumView EnumValue="@Column.Width" EnumValueChanged="x => SetWidthValue(x)" IsReadOnly="IsReadOnly" />

@code {
    [Parameter]
    public Column Column { get; set; }

    [Parameter]
    public bool IsReadOnly { get; set; } = false;

    private void SetWidthValue(ColumnWidth width)
    {
        Column.Width = width;

        if (Column.Width == ColumnWidth.Value)
        {
            Column.Value = 1;
        }
        else
        {
            Column.Value = null;
        }

        StateHasChanged();
    }
}
